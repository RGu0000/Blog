- if @comments.any?
  %h3 Comments
  - @comments.each do |comment|
    - if comment.id.present?
      %h2
        %small= "Added by #{comment.author.name} on #{comment.created_at.strftime("%d/%m/%Y %H:%M")}"
      - if current_user.present? && current_user.id == comment.author_id
        %p{style: "color:red"}= comment.body
        =link_to "Delete your comment", article_comment_path(@article.id, comment.id), class: "btn btn-danger", method: :delete, data: { confirm: 'Are you sure?' }
      - else
        = comment.body
-else
  %h3 No comments has been added yet. You still can be first!
